# Supabase project (client-safe values are read at build time)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Private server-side secrets (NEVER commit real values / NEVER used by the mobile client)
SUPABASE_SERVICE_ROLE_KEY=
AI_API_KEY=
AI_BASE_URL=
AI_MODEL=
API_BASE_URL=http://localhost:3900

# Expo / EAS
EXPO_ACCESS_TOKEN=
